// write a js program that concatenates two strings except their first character

const concatenate = (string,string1) => 
    string.slice(1)+string1.slice(1);
console.log(concatenate("Gone","goood"));